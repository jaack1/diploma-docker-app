apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "netology-diploma.fullname" . }}-test-connection"
  labels:
    {{- include "netology-diploma.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "netology-diploma.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
